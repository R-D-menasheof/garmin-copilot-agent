name: vitalis
description: Vitalis — personal health & nutrition tracker.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.0
  go_router: ^14.0.0
  http: ^1.2.0
  flutter_secure_storage: ^9.2.0
  image_picker: ^1.1.0
  intl: ^0.19.0
  # isar: ^3.1.0+1          # TODO: re-enable when offline sync is implemented
  # isar_flutter_libs: ^3.1.0+1
  health: ^10.2.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  # isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true
